{
  "dependencies": {
    "firebase": "^9.21.0",
    "three": "^0.151.3"
  },
  "devDependencies": {
    "vite": "^4.3.1"
  },
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "publish": "vite build && git add * && git commit -a -m \"Publish commit\" && git push && firebase deploy"
  }
}
